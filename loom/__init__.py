"""Loom — a hybrid local/cloud multi-agent CLI coding assistant.

A strong cloud model orchestrates and plans; specialized subagents (mostly local
Ollama models) handle isolated, bounded subtasks in separate context windows and
return only summaries — preserving the orchestrator's context quality.
"""

__version__ = "0.1.0"
